package transform;

public interface ILogicalTransformer {
	/*
	//logical commands, other can be implemented by these
	//Returns State because two intervals might be updated
    List<LatticeElement> gt(Interval other);
    List<LatticeElement> gt(Top other);
    List<LatticeElement> gt(Bottom other);
    List<LatticeElement> gt(PositiveInf other);
    List<LatticeElement> gt(NegativeInf other);

    List<LatticeElement> ge(Interval other);
    List<LatticeElement> ge(Top other);
    List<LatticeElement> ge(Bottom other);
    List<LatticeElement> ge(PositiveInf other);
    List<LatticeElement> ge(NegativeInf other);
    */
 
}
